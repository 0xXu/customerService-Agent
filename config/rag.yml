chat_provider: gemini
chat_model_name: models/gemini-2.5-flash-lite
embedding_provider: gemini
embedding_model_name: models/gemini-embedding-001
temperature: 0.7

# OpenAI-compatible custom endpoint. Leave empty for official OpenAI.
openai_base_url:

# DeepSeek uses an OpenAI-compatible chat API. It is only used when
# chat_provider is set to deepseek.
deepseek_base_url: https://api.deepseek.com
